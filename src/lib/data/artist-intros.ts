/** Tone-focused prose bios for artist pages — SEO pillar content.
 *  Each artist gets 2 paragraphs focused on tone signature and gear history. */
export const ARTIST_INTROS: Record<string, string[]> = {};
