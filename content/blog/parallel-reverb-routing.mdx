---
title: >-
  Parallel Reverb Routing: Why Running Two Reverbs Side by Side Solves Problems
  That Series Can't
seo_title: "Parallel Reverb Routing: What Series Can't Solve"
description: >-
  Stack two reverbs in series and the second one processes the mud the first
  created. Parallel routing keeps them independent and solves the buildup at
  its source.
date: '2026-04-19'
updated: '2026-08-03'
author: Margot Thiessen
author_slug: margot-thiessen
category: signal-chain
tags:
  - reverb
  - signal-chain
  - parallel-routing
  - ambient
  - two-reverbs
  - stacking
  - modeler
takeaways:
  - >-
    Series routing feeds the first reverb's wet output into the second reverb's
    input, so the second one processes accumulated low-mid content it did not
    create. Parallel routing feeds both reverbs the same dry signal, so neither
    ever sees the other's output.
  - >-
    Parallel decouples mix from processing. In series, lowering reverb A's mix
    also lowers the input level reaching reverb B and changes its behavior. In
    parallel, you can rebalance either reverb without altering the other's
    character.
  - >-
    Set the reverb blocks on parallel paths to 100 percent wet and let the
    mixer handle the dry. Leaving each block at 30 percent mix sends a second
    and third copy of the dry signal down the chain, which sounds like the
    reverbs got quieter when what actually happened is the dry got louder.
  - >-
    Parallel routing introduces a problem series does not have: two reverbs fed
    from the same dry signal are correlated, so their early reflections can
    comb-filter when summed to mono. Check the mono sum before you commit.
  - >-
    Parallel is still forward-only routing. Both paths move downstream and
    re-merge, which is why a parallel path cannot produce a growing or
    regenerating reverb — that requires a physical send-to-return loop.
faq:
  - q: What is parallel reverb routing?
    a: >-
      A signal chain configuration where the dry guitar signal splits before any
      reverb processing, feeds two reverb effects independently, and combines
      their outputs at a mixer. Neither reverb processes the other's output, so
      low-mid buildup from one reverb does not enter the other's processing
      chain.
  - q: When should I use parallel reverb routing instead of series?
    a: >-
      When both reverbs need high mix levels with full frequency content, where
      high-passing would remove what makes each one useful. Also when you want
      each reverb to hold its own stereo field, or when the mud in series
      persists after high-pass filtering because the problem frequencies are in
      the signal fundamentals.
  - q: Can you do parallel reverb routing on a standard pedalboard?
    a: >-
      Yes, with a signal splitter and a stereo mixer, or with a purpose-built
      parallel loop unit. The Source Audio Ventris runs two reverb engines in
      parallel inside one enclosure, which is the simplest hardware answer. A
      passive splitter feeding two reverbs into a mixer works with any pedals.
  - q: How do I set up parallel reverb in Helix?
    a: >-
      Add a Split block where you want to branch the signal, route each path to
      its own reverb block, and add a Mixer block at the output to combine
      them. Set each path's level at the Mixer to 0 dB and adjust from there.
      Set the reverb blocks themselves to 100 percent wet so the mixer is the
      only place dry and wet are balanced.
  - q: Why do my parallel reverbs sound thinner when the sound guy sums to mono?
    a: >-
      Because both reverbs are fed from the same dry signal, their early
      reflections are correlated rather than independent. Correlated signals
      arriving a few milliseconds apart comb-filter when summed. Offsetting one
      reverb's pre-delay by 10 to 20 ms decorrelates them and largely fixes it.
  - q: What is the difference between parallel routing and the Source Audio Ventris?
    a: >-
      The Ventris runs two independent reverb engines in parallel inside a
      single enclosure with a built-in mixer — parallel routing pre-packaged in
      hardware. The limitation is that both engines are Ventris algorithms. A
      splitter, two reverb pedals, and a mixer give the same architecture with
      any algorithms you choose.
readingTime: 8 min
image: /images/blog/parallel-reverb-routing.jpg
image_alt: >-
  Two reverb pedals connected side by side to a signal splitter and a stereo
  mixer on a pedalboard, with a modeler screen showing a split signal path and
  a mixer block
---

The [stacking reverbs guide](/blog/stacking-reverbs-guide) identified the mechanism behind reverb mud: low-mid energy accumulates below 250 Hz when the first reverb's wet signal feeds into the second reverb's input. You can address that in series with a high-pass filter on the second reverb's input, and for most applications that's enough. But there's a routing approach that solves the problem structurally rather than correcting it after the fact.

Parallel reverb routing runs two reverb effects independently, fed from the same dry signal, and combines their outputs at a mixer rather than passing one's output into the other's input.

The difference matters most when you need both reverbs to do substantial work, when each needs real depth or density that high-pass filtering would undermine.

---

## What Series Routing Does

In a series chain, the signal flows through the first reverb, and the combined wet-plus-dry output of that reverb then enters the second reverb's input.

This means: the second reverb processes not just your guitar signal, but the entire tonal character of the first reverb, including its low-mid content, its pre-delay behavior, and its early reflections. The second reverb generates its own low-mid content on top of the first reverb's low-mid content. The result is a tonal accumulation in the 150–350 Hz range that no individual reverb setting can prevent.

When I first tried using a Strymon BigSky followed by a small room reverb pedal, I spent forty minutes assuming the room reverb's pre-delay was wrong, then its decay time, then its mix level. None of those adjustments solved it. The mud was in the signal entering the room reverb, not in how the room reverb was processing it. The problem was upstream.

---

## What Parallel Routing Does

In a parallel chain, the dry guitar signal splits before any reverb processing. Reverb A receives only the dry signal. Reverb B receives only the dry signal. Their outputs are combined at a mixer, not at the input of one or the other.

The result: each reverb is processing the same clean, uncolored dry signal. Whatever low-mid content reverb A generates in its wet output stays in reverb A's output. It never enters reverb B's processing chain. The two reverbs are acoustically independent even though their outputs combine.

The combined output still has the low-mid content of both reverbs, but there's an important difference: you can control each reverb's mix independently at the mixer stage, which lets you reduce the low-mid contribution of either reverb without affecting its decay character. In series, reducing mix on reverb A also reduces the input level that reverb B receives, which changes reverb B's behavior. In parallel, mixing is fully decoupled from processing.

---

## When Parallel Routing Is Worth the Effort

Series with a high-pass filter is simpler to set up and adequate for most applications. Consider parallel when:

- Both reverbs need to be at 40%+ mix with full low-end presence (shimmer reverb + room reverb, for example, where high-passing would remove what makes each one useful)
- You want each reverb to maintain its own independent stereo field without bleed from the other
- You're using one reverb as a pre-delay and one as a long decay, and you need the decay reverb to respond to the dry guitar rather than to the pre-delayed signal
- The mud persists after high-pass filtering because the low-mid issue is in the fundamentals you can't cut

Series with a high-pass filter is adequate when: the mix levels are moderate (under 35% on the first reverb), the first reverb's character doesn't depend on its low end, or the routing simplicity is worth the mild compromise.

---

## How to Set Up Parallel Routing

### On a Modeler (Helix, Quad Cortex, HX Stomp)

Modelers make parallel routing straightforward because they support multiple signal paths natively.

**Helix / HX Stomp:**
Add a Split block at the point where you want to branch. Route Path A to reverb 1. Route Path B to reverb 2. Use a Mixer block at the output to combine the paths. Set each path's volume at the Mixer to taste (typically 0dB on both), then adjust individual reverb mix controls.

The Helix's Mixer block also lets you pan the two reverb outputs before combining, which can give each reverb its own spatial placement in the stereo field before they mix at the output.

**Set both reverb blocks to 100% wet.** This is the step that gets skipped, and skipping it produces a specific confusion. A reverb block sitting at its usual 30% mix passes 70% dry along with its wet signal. Put one of those on each parallel path and the mixer receives two additional copies of the dry guitar, which then sum with whatever dry you already had. The reverbs have not gotten quieter. The dry has gotten roughly three times louder, and every adjustment you make to the reverb mix controls will feel like it is doing less than it should. Let the paths carry wet only, and let the mixer decide how much dry sits underneath.

**Quad Cortex:**
The QC's grid layout supports parallel routing by placing blocks on separate rows. Rows 1 and 2 can run independently; insert a merge node at the output to combine them. Assign reverb 1 to Row 1 and reverb 2 to Row 2, with the merge node bringing both outputs to a single signal.

### On a Hardware Pedalboard

This requires a signal splitter and a stereo mixer, or a purpose-built parallel loop unit. Practical options:

**Source Audio Ventris:** A single stereo reverb pedal that runs two independent reverb engines in parallel internally, with a stereo mixer built in. This is the most plug-in-play solution; you get parallel routing within a single enclosure. The tradeoff: you're limited to Ventris's reverb algorithms for both engines.

**Splitter + two reverbs + mixer:** A passive signal splitter (or a buffer-based active splitter like the Lehle Splitter) feeds two separate reverb pedals; their outputs combine at a passive or active stereo mixer (Saturnworks, Xotic X-Blender, etc.). This works with any two reverb pedals and gives full algorithm flexibility. It requires three extra pieces of hardware and adds cable complexity.

**Loop switcher with parallel mode:** Some switchers (Boss ES-8, Gigrig G2) support parallel routing in their loop configuration. If you already run a loop switcher, check whether it supports parallel sends before buying a separate splitter.

---

## Suggested Configurations

### Shimmer + Room

A common ambient pairing: a shimmer reverb (BigSky Shimmer, Meris Mercury7, EHX Cathedral shimmer mode) running in parallel with a small room reverb (BigSky Room, Strymon Flint reverb at shorter decay, TC Hall of Fame mini).

In series, the shimmer's pitch-shifted harmonics feed into the room reverb and create a phasiness in the high-mids that sounds synthetic. In parallel, the room reverb processes only the dry guitar, and the shimmer adds a separate halo above the dry-plus-room signal. The combination sounds like a guitar in a room with a shimmer source behind it rather than like a shimmer pedal running through a room simulation.

Settings to start:

| | Shimmer Reverb | Room Reverb |
|---|---|---|
| Decay | 8–12 seconds | 0.8–1.5 seconds |
| Mix | 25–35% | 30–40% |
| Pre-delay | 0ms | 15–25ms |
| Low cut | 200 Hz on wet | 150 Hz on wet |

### Hall + Pre-Delay

A hall reverb with a very short pre-delay reverb running in parallel is a different approach: the pre-delay reverb adds initial reflection detail that a long hall reverb smears. In series, the pre-delay reverb feeds into the hall reverb and the reflections extend into the hall's decay, which sounds washed. In parallel, the early reflection texture and the long hall decay are independent, and the guitar's attack reads through both clearly.

---

---

## The Cost Nobody Mentions: What Happens in Mono

Series routing has an accumulation problem. Parallel routing has a correlation problem, and it hides until someone else is holding the faders.

Both reverbs are fed from the same dry signal. That is the entire point — it is what keeps them independent. But it also means their early reflections begin from an identical source at an identical moment, and two versions of the same event arriving a few milliseconds apart is the exact recipe for comb filtering. In stereo, spread across a room, you never hear it; the two halos occupy different places and the ear reads them as space. Summed to mono — a phone speaker, a mono PA feed, a laptop — the shared content interferes with itself and specific frequencies cancel.

What it sounds like is not a dropout. It is subtler and more disheartening than that: the reverb that felt like a cathedral in your headphones arrives as a hollow, slightly nasal wash, with a scooped quality around the early reflections that no EQ move puts back, because the energy is not attenuated, it is gone.

The fix is decorrelation, and it is one parameter:

| Move | Setting | Why |
|---|---|---|
| Offset pre-delay | 10-20 ms difference between the two reverbs | Their early reflections no longer arrive together, so there is nothing to cancel |
| Vary decay | Do not run both at identical decay times | Identical decays keep the tails correlated too |
| Check the sum | Collapse to mono and listen before you commit | Thirty seconds, and it is the only way to know |

The pre-delay offset costs nothing musically. In the Shimmer and Room configuration above, the room reverb is already at 15-25 ms while the shimmer sits at 0 ms, which is why that particular pairing tends to survive a mono fold-down without anyone having to think about it. Two reverbs both at 0 ms pre-delay is the configuration to be suspicious of.

---

## What Parallel Routing Still Cannot Do

Parallel is not the opposite of series. Both are forward-only: paths split, travel downstream, and re-merge, and at no point does anything arrive somewhere it has already been. That distinction is invisible until you want a reverb that *grows* rather than decays, or a tail that re-processes itself on every pass, and then it becomes the whole story — those effects need a signal to re-enter a block it already left, which no split-and-merge topology provides. Building one requires [a physical cable from a send back to a return](/blog/modeler-send-return-loop-tricks-beyond-shimmer), which is a different technique that happens to live one menu away.

Worth knowing which problem you have. Accumulation is a series problem and parallel solves it. Regeneration is not a problem at all, it is a sound, and neither routing gives it to you.

---

The [reverb pedal pairings guide](/blog/reverb-pedal-pairings) covers specific hardware combinations in series that work well. [Stacking reverbs](/blog/stacking-reverbs-guide) covers the series approach with high-pass filtering as the primary technique. Parallel routing is the third option: more setup, more flexibility, and the only approach that solves the accumulation problem at its source rather than managing it after the fact.

---

<SaveThisTone
  title="Want an ambient clean starting point?"
  description="Pick a recipe close to the tone in this post and tweak from there. Faster than building from a blank patch."
  href="/browse?tag=clean"
  ctaLabel="Browse ambient clean recipes"
/>
