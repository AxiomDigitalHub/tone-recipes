---
title: 'How to Level-Match All Your Presets: Helix, Quad Cortex, and Fractal'
seo_title: "How to Level-Match All Your Modeler Presets"
description: >-
  Volume jumps between presets are a solved problem. Here's the exact workflow
  for Helix, Quad Cortex, and Fractal Axe-Fx to get consistent output levels
  across your entire preset library.
date: '2026-04-08'
updated: '2026-09-04'
author: Sean Nakamura
author_slug: sean-nakamura
category: "quick-fixes"
tags:
  - preset level matching
  - helix volume
  - quad cortex volume
  - modeler presets
  - helix output block
  - volume matching
  - modeler workflow
readingTime: 7
takeaways:
  - >-
    Normalise at the Output block, not the amp block's Channel Volume. Channel
    Volume changes how hard the amp model is driven, which is a tone change
    wearing a level control's clothing.
  - >-
    Set one reference preset and never touch it again. Every other preset gets
    matched to that anchor, which is what keeps the library consistent as you
    add to it.
  - >-
    Match perceived loudness by ear rather than peak meters. A compressed lead
    and an open clean patch can read identically on a meter and sound several
    decibels apart.
  - >-
    Always re-check the Output trim on any preset you download. It was built
    around somebody else's gain structure and almost never lands where yours
    does.
  - >-
    This procedure fixes preset-to-preset jumps only. If the problem appears
    when you change guitars rather than when you change presets, the mismatch
    is ahead of the amp model and an output trim cannot reach it.
faq:
  - q: Should I use a decibel meter app to level-match my presets?
    a: >-
      A meter helps as a starting reference, but ultimately you are matching
      perceived loudness, which is not purely a dB measurement. Use it as a
      rough guide and then trust your ears for the final trim.
  - q: Why do my presets sound matched at home but different at the venue?
    a: >-
      The room and the monitoring setup change how loudness is perceived. A
      bright stage makes some frequencies feel louder, and a dark room absorbs
      high frequency content so treble-heavy patches seem quieter. Normalise in
      an environment similar to where you play, then re-check at soundcheck.
  - q: Is there an automatic volume normalizer on Helix, Quad Cortex, or Fractal?
    a: >-
      Not natively. Fractal's Global Blocks let you share parameters across
      presets but do not auto-normalize levels. The manual trim method is still
      the standard approach on every platform.
  - q: Do I need to re-level presets I download from the cloud?
    a: >-
      Yes, every time. Cloud presets are built by players with different gain
      structures and different output levels, so check the Output block trim on
      any imported preset before you use it in performance.
  - q: What output level should I send to the PA?
    a: >-
      A common target is around -18 to -12 dBFS average for your clean tone,
      with headroom for peaks. Your job is a consistent, manageable signal from
      preset to preset. The overall level in the PA is the engineer's.
  - q: My presets are level but one guitar sounds wrong through all of them. Why?
    a: >-
      Because that is a different mismatch. Output trims sit at the end of the
      chain, so they change loudness without changing how hard the amp model is
      being driven. A quieter guitar gets less saturation out of the same
      block, and no amount of output trim puts that back.
image: /images/blog/level-match-modeler-presets.jpg
image_alt: >-
  A floor modeler's output block screen showing a level parameter, with studio
  monitors and a guitar on a stand behind it
---

Volume jumps between presets are one of the most common live performance problems with modelers, and one of the most solvable. The fix is different on each platform, but the principle is the same: you need a volume reference point that's independent of your tone-shaping chain.

Here's how to do it on Helix, Quad Cortex, and Fractal Axe-Fx.

---

## Why Presets Have Different Volumes

The short answer: every amp model, drive pedal, and effect combination produces a different output level. A clean Vox AC30 model at 5 is quieter than a high-gain Mesa Rectifier model at 5. A wet reverb-heavy preset sounds softer than a dry preset at the same measured output, because your ear perceives the diffuse signal differently.

When you build presets in isolation (dialing in each one to sound good on its own), you calibrate each one to "sounds good" without calibrating them to each other. The result is a set of presets that each sound correct individually and chaos when you switch between them.

The solution is a dedicated output trim at the end of each preset, set independently of the tone chain, that you use to normalize levels.

---

## The Reference Preset Method

Before platform-specific steps, one universal approach: create a reference preset first.

Pick your most-used clean patch as the reference. Set it to your target output level (whatever hits your PA or FRFR at the volume you want). Every other preset gets matched to this one by ear using a sine wave or pink noise reference, or more practically, by playing the same chord on both and adjusting until they match.

Once the reference is set, you have a consistent anchor. Don't touch its output trim afterward.

---

## Helix: Output Block Level Control

The cleanest method on Helix is the **Output block**.

Every Helix preset has an Output block at the end of the signal chain. It defaults to 0dB. Use this as your per-preset trim:

1. Open your loudest preset in HX Edit.
2. Click the Output block.
3. This is your loudest preset, so set its Output level as your reference; call this 0dB.
4. Switch to each other preset.
5. Play the same chord (root-5th power chord works well; consistent harmonic content).
6. Adjust the Output block level on each preset until it matches the reference preset.
7. A preset that's quiet by default might need +2 or +3dB at the Output. A high-gain patch that's loud by default might need −3 or −4dB.

**Why the Output block, not the amp block's Channel Volume?** The Channel Volume changes how the amp model is being driven; it affects the tone and the feel, not just the level. The Output block is a clean trim that sits after all the tone processing. It's the right tool for normalization.

**Global vs. per-preset:** The Helix also has a Global EQ and Global Settings output level, but these affect everything. Don't use them for normalization; they change your overall output to the PA, not individual preset levels.

---

## Quad Cortex: Scene Volume and Output Block

The Quad Cortex has two places to normalize: the **Output block** within each preset, and **Scene Volume** if you're using Scenes.

**For preset-level normalization:**

1. Every preset on the Quad Cortex ends with an Output block. Tap it.
2. The Output block has a Level parameter; this is your trim.
3. Use the same method as Helix: set your loudest preset as reference at 0dB, then bring other presets up or down to match.
4. The Neural DSP documentation calls this the "Output Level"; it's a clean gain stage after all processing.

**For Scenes within a preset:**

If you're using Scenes (and for live worship or live performance with multiple sounds in one preset, you should be), each scene can have its own volume level. In the Quad Cortex:

1. Enter Scene mode.
2. Assign the Output block Level to a Scene controller.
3. Set the level per scene independently.

This lets you store a verse volume, a chorus volume, and a bridge lead boost all within a single preset, with scene-level control that doesn't require you to reach for the expression pedal. The scene-to-scene transition is immediate and gapless.

**Expected Cortex quirk:** When you first load a preset you've been editing level-by-level, the Output block trim is easy to accidentally reset if you're copying presets from the cloud or importing them from Neural DSP's preset library. Check the Output block level any time you import a preset; it may have been set for someone else's gain structure.

---

## Fractal Axe-Fx III and FM9: Output Mixer and Per-Preset Level

Fractal's architecture is the most granular of the three platforms. The relevant tools:

**Per-Preset Output Trim:**

The Axe-Fx III has an **Out1 Level** control (and Out2 for a second output pair) in the I/O menu. This is per-preset and acts as a master trim before the output stage.

1. Navigate to the I/O settings within each preset.
2. Find Out1 Level.
3. Use the same reference method: match each preset to your loudest clean reference.
4. Typical values: clean patches may need +1 to +3dB, high-gain patches may need −2 to −4dB.

**Output Mixer:**

For more complex routing (if you're sending different signals to FOH and to a monitor amp), the Output Mixer lets you set independent levels per output. This is more advanced than preset-level normalization, but useful if you're splitting wet to FOH and dry to a real amp.

**Scene Level on Axe-Fx:**

Like the Quad Cortex, the Axe-Fx supports Scenes. You can control mixer level per scene using a modifier assigned to the scene controller. For lead boosts, this is the correct architecture: keep the amp model and drive settings the same between scenes, and raise the Output Mixer level for your lead scene by +3dB.

---

## A Practical Normalization Session

Here's the workflow I use when building out a new preset library:

1. **Set a reference first.** Open a clean patch you're happy with. This is your 0dB reference. Don't change it.
2. **Work through presets in order of type:** all cleans first, then all leads, then all ambient/effects-heavy.
3. **Use headphones for the comparison.** Your FRFR or monitors can color the perception. A flat headphone response gives you a more accurate A/B.
4. **Play the same thing on each preset.** Same chord, same position on the neck, same picking force. Consistency in the test signal means consistency in the result.
5. **Match perceived loudness, not peak meters.** A highly compressed lead tone and a clean tone can read the same on a peak meter but sound very different in loudness. Match what you hear, not what the meter says.
6. **Document your trims in a spreadsheet or note.** If you rebuild a preset later, you'll know what trim value you set and why.

One session of an hour or two normalizes an entire preset library. After that, maintenance is per-preset as you add new ones.

---

## When Leveling Is Not the Fix: Two Guitars, One Bank

Everything above solves preset-to-preset jumps. There is a neighbouring problem it cannot touch, and it is worth naming because the symptoms overlap enough to waste an afternoon.

If your presets are consistent with each other but the whole bank sounds wrong the moment you pick up your *other* guitar, output trims are the wrong tool. Here is why: the Output block sits at the very end of the chain, after the amp, after the cab, after everything. It changes how loud the finished sound is. It does not change how hard the amp model is being hit.

And the amp model is where the difference actually lands. Amp models respond to input level the way a real amp front end does, so a lower-output guitar gets less saturation, less compression, and a softer attack out of the same block. Raise the Output trim afterwards and you get all of that, louder. The tone you were complaining about is untouched.

The distinction in one line:

| Symptom | Where the fix lives |
|---|---|
| Preset A is louder than preset B | Output block. This article. |
| Same preset, different guitar, feels soft and undriven | Ahead of the amp model. A gain block first in the chain. |

If it is the second one, [leveling two guitars through one rig](/blog/one-modeler-two-guitars-different-output-levels) covers the four places you can compensate and why only one of them is per-preset. And if the hotter of your two guitars runs active pickups, [the input pad decision](/blog/modeler-input-pad-when-you-need-it) is a third question again, sitting ahead of both.

Do the output leveling first regardless. A consistent bank is the reference you need before you can hear anything else clearly.

<SaveThisTone
  title="Pick a tone to chase"
  description="Every recipe in the catalog has the chain, exact settings, and a preset for Helix or Katana."
  href="/browse"
  ctaLabel="Browse the catalog"
/>
